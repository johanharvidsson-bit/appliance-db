# Development-only PostgREST schema exposure for the isolated VPS dev stack.
# Keep real database URIs, JWT secrets and tokens in the untracked stack .env.
PGRST_DB_SCHEMAS=public,api_public
PGRST_DB_ANON_ROLE=anon
PGRST_DB_MAX_ROWS=1000
