# ── Supabase ───────────────────────────────────────────────────────────────────
# Project URL — find it at: supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Service role key — find it at: supabase.com/dashboard/project/_/settings/api
# WARNING: keep this secret — it bypasses row-level security
SUPABASE_SERVICE_KEY=eyJhbGci...

# Direct Postgres connection string — required only for DDL migrations (db/apply_migration.py)
# Find it at: supabase.com/dashboard/project/_/settings/database -> Connection string -> Pooler
# Use Session mode (port 6543), not Transaction mode
SUPABASE_DB_URL=postgresql://postgres.xxxxxxxxxxxxxxxxxxxx:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# ── AI / LLM ──────────────────────────────────────────────────────────────────
# Anthropic API key — used by article_generator.py and enrich_error_codes.py
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ── External APIs ─────────────────────────────────────────────────────────────
# Serper (Google Search API) — used by enrich_error_codes.py
# https://serper.dev/api-key
SERPER_API_KEY=

# Worker pipeline pilot (development only). Never copy production values here.
APP_ENV=development
ENVIRONMENT=development
SITE_ID=appliance-repair-base
REPAIRBASE_SECURITY_TEST_DB_URL=
# Required together for an external development DSN; exact identity, no secrets.
REPAIRBASE_DEV_DB_HOST=dev-project.pooler.example.net
REPAIRBASE_DEV_DB_NAME=repairbase_worker_dev
REPAIRBASE_DEV_DB_USER=repairbase_worker
WORKER_DOCUMENT_STORAGE=./data/source-ingestion
WORKER_REPORT_OUTPUT=./reports
WORKER_HTTP_USER_AGENT=RepairBaseSourceIngestion/1.0
WORKER_TIMEOUT_SECONDS=15
WORKER_RETRIES=2
WORKER_RATE_LIMIT_SECONDS=1
WORKER_MAX_DOCUMENT_BYTES=15000000

# Scrape.do proxy API key — reserved for future bot-protected scraping targets
# Not used by any active pipeline script. Safe to leave blank.
# https://scrape.do
SCRAPE_DO_API_KEY=

# ── Scraper behaviour ─────────────────────────────────────────────────────────
# Seconds to wait between HTTP requests (default: 2.0)
REQUEST_DELAY_SECONDS=2.0

# Max retries on transient HTTP errors (default: 3)
MAX_RETRIES=3

# Download PDF manuals to disk for pdf_extractor (true/false)
DOWNLOAD_MANUALS=true

# Local directory for downloaded manuals (relative to project root)
MANUAL_DOWNLOAD_DIR=./data/manuals

# ── Tesseract OCR ─────────────────────────────────────────────────────────────
# Path to tesseract binary — required by image_extractor.py
# Windows default (install from: https://github.com/UB-Mannheim/tesseract/wiki)
TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
# Linux / macOS:
# TESSERACT_CMD=/usr/bin/tesseract

# ── Logging ────────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_DIR=./logs
